%section.albumbar.carousel
  .container
    %h5= @lightbox.name
    %ul.event-list
      - @lightbox.photos.each do |photo|
        = render partial: "photos/photo_search", object: photo
    / %ul.menu
    /   %li.nameof
    /     %a NAME